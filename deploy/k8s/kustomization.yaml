apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: iep-progress-tracker
resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - pvc.yaml
  - postgres.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
images:
  - name: iep-progress-tracker
    newTag: "0.4.0"
