apiVersion: v1
kind: Service
metadata:
  name: iep-progress-tracker
  namespace: iep-progress-tracker
  labels:
    app.kubernetes.io/name: iep-progress-tracker
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: iep-progress-tracker
  ports:
    - name: http
      port: 80
      targetPort: http
